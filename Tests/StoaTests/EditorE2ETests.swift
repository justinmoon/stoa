import XCTest
@testable import StoaKit

final class EditorE2ETests: XCTestCase {
    func testEditorEditsAndSavesFile() throws {
        throw XCTSkip("Embedded editor e2e runs via `just test-embed`.")
    }
}
